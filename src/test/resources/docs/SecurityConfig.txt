🇩🇪 Dokumentation – SecurityConfig
Diese Klasse konfiguriert die Spring‑Security‑Einstellungen der Anwendung.
Sie definiert Authentifizierung, Autorisierung, Passwort‑Encoding und die verwendete UserDetailsService‑Implementierung.

Hauptfunktionen
1. SecurityFilterChain
Deaktiviert CSRF (typisch für stateless REST‑APIs).

Definiert Zugriffsregeln:

/api/public/** → öffentlich zugänglich.

/api/admin/** → nur für Benutzer mit Rolle ADMIN.

Alle anderen Endpunkte → Authentifizierung erforderlich.

Aktiviert HTTP Basic Authentication.

2. UserDetailsService
Verwendet die benutzerdefinierte Implementierung CustomUserDetailsService.

Verantwortlich für das Laden von Benutzerdaten aus der Datenquelle.

3. PasswordEncoder
Nutzt BCryptPasswordEncoder für sichere Passwort‑Hashing‑Verfahren.

4. AuthenticationManager
Wird aus der Spring‑Konfiguration übernommen.

Ermöglicht Authentifizierungsvorgänge in Services oder Controllern.

*******
🇬🇧 Documentation – SecurityConfig
This class configures Spring Security for the application.
It defines authentication, authorization rules, password encoding, and the custom UserDetailsService.

Main Features
1. SecurityFilterChain
Disables CSRF (common for stateless REST APIs).

Defines access rules:

/api/public/** → accessible without authentication.

/api/admin/** → requires ADMIN role.

All other endpoints → require authentication.

Enables HTTP Basic authentication.

2. UserDetailsService
Uses the custom implementation CustomUserDetailsService.

Responsible for loading user data from the data source.

3. PasswordEncoder
Uses BCryptPasswordEncoder to securely hash passwords.

4. AuthenticationManager
Obtains the authentication manager from Spring’s configuration.

Allows authentication logic inside services or controllers.

******
🇷🇺 Документация – SecurityConfig
Этот класс настраивает Spring Security для приложения.
Он определяет правила аутентификации, авторизации, кодирование паролей и использование кастомного UserDetailsService.

Основные функции
1. SecurityFilterChain
Отключает CSRF (обычно для REST‑API без состояния).

Настраивает правила доступа:

/api/public/** → доступно всем.

/api/admin/** → доступ только для пользователей с ролью ADMIN.

Все остальные запросы → требуют аутентификации.

Включает HTTP Basic аутентификацию.

2. UserDetailsService
Использует кастомную реализацию CustomUserDetailsService.

Отвечает за загрузку данных пользователя из источника данных.

3. PasswordEncoder
Применяет BCryptPasswordEncoder для безопасного хеширования паролей.

4. AuthenticationManager
Получает менеджер аутентификации из конфигурации Spring.

Позволяет выполнять аутентификацию в сервисах или контроллерах.